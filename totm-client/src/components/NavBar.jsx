const NavBar = () => {
  return (
    <nav>
      <span>TOTM</span>
      <span>
        <button>LOGIN</button>
        <button>REGISTER</button>
      </span>
    </nav>
  );
};

export default NavBar;